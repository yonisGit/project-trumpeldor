<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bguarts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".camera_page" />
        <activity android:name=".group_creation" />
        <activity android:name=".main_game" />
        <activity android:name=".multiple_choice_game" />
        <activity android:name=".Puzzle_page" />
        <activity android:name=".SlidingPuzzle" />
        <activity android:name=".TrackPointInformation" />
        <activity android:name=".TrackPointPage" />
        <activity android:name=".FinishPath" />
        <activity android:name=".Description" />
        <activity android:name=".CampusArt"></activity>
    </application>

</manifest>